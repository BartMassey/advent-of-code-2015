The trick here is to treat the input passwords as
overlapping groups of letters, and look for groups with the
right properties.

The Haskell code uses `tile` from the solution library
(`Soln`) to make the necessary groupings.
